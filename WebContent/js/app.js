const splash = document.querySelector('.splash');

document.addEventListener('DOMContentLoaded', (e)=>(
            setTimeout(()=>(          
        splash.classList.add('displayn')
        ),2000)
));